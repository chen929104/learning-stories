/**
 * 1 箭头函数写法比价简单，对于没有参数可以用（）
 * 有一个参数可以不用括号,多个参数用法一样
 * 直接一句可以省略大括号
 *
 * 2 没有this,会劫持当前作用域来来继承this，this在写的时候就确定了。
 *
 * 3 不会因为apply call bind 改变this绑定
 *
 * 4 没有prototype
 *
 * 5 不能作为构造函数使用
 *
 * 6 没有 arguments 不过现在建议使用 ... 而不是使用arguments
 *
 * 7 不能用做Generator 不能使用yeild关键字
 */
